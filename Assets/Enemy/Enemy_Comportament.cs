using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy_Comportament : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    //Criar Classe de estado para o Enemy, para poder colocar ele em: Patrulha, Chasing, Stopped. (Plase Make This)
    private void OnTriggerEnter2D(Collider2D collision)
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
